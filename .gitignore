
/Padrão de qualidade - Felixo System Design

# Segredos: a chave do OpenRouter nunca deve ser versionada.
orctl/.env
.env

# Cache local do catálogo de modelos (gerado em runtime).
orctl/.models_cache.json

# Artefatos Python
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
build/
dist/
