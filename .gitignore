# Pasta local do padrão de qualidade (repositório próprio, não versionada aqui).
/Padrão de qualidade - Felixo System Design/

# Segredos: as chaves do OpenRouter nunca devem ser versionadas.
openia/keys.json
openia/.env
.env

# Cache local do catálogo de modelos (gerado em runtime).
openia/.models_cache.json

# Artefatos Python
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
build/
dist/
